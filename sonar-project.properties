Enter file contents here# must be unique in a given SonarQube instance
sonar.projectKey=TG
# this is the name displayed in the SonarQube UI
sonar.projectName=TGDUEngine
sonar.projectVersion=0.2
sonar.host.url=http://ec2-52-32-54-95.us-west-2.compute.amazonaws.com:9000
sonar.login=admin
sonar.password=admin
sonar.jdbc.url=jdbc:h2:tcp://ec2-52-32-54-95.us-west-2.compute.amazonaws.com:9092/sonar
sonar.jdbc.username=sonar
#sonar.jdbc.password=sonar
 
# Path is relative to the sonar-project.properties file. Replace "\" by "/" on Windows.
# Since SonarQube 4.2, this property is optional if sonar.modules is set. 
# If not set, SonarQube starts looking for source code from the directory containing 
# the sonar-project.properties file.
#sonar.sources=src/main
 
# Encoding of the source code. Default is default system encoding
#sonar.sourceEncoding=UTF-8

#JIRA details

# Comma-separated paths to directories with sources (required)
sonar.sources=src/main

# Comma-separated paths to directories with tests (optional)
sonar.tests=src/test

# Encoding of the source files
sonar.sourceEncoding=UTF-8

# Language
sonar.language=java

#Tells SonarQube to reuse existing reports for unit tests execution and coverage reports - depricated
#sonar.dynamicAnalysis=reuseReports

sonar.binaries=target/classes
